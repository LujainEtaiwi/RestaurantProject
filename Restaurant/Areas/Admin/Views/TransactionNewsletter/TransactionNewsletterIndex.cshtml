@model IEnumerable<Restaurant.Models.TransactionNewsletter>

@{
    ViewData["Title"] = "TransactionNewsletterIndex";
}

<div class="pagetitle">
    <h1>Transaction Newsletter Table</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Home">Home</a></li>
            <li class="breadcrumb-item active">Transaction Newsletter</li>
        </ol>
    </nav>
</div>


<hr />



<section class="section">
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">

                    <table class="table datatable index-table text-break">
                        <thead>
                            <tr >
                                <th>
                                    @Html.DisplayNameFor(model => model.TransactionNewsletterEmail)
                                </th>
                            </tr>
                        </thead>
                        <tbody >
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="align-middle">
                                        @Html.DisplayFor(modelItem => item.TransactionNewsletterEmail)
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>
