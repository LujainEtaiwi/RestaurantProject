@model IEnumerable<Restaurant.Models.MasterContactUsInformation>

@{
    ViewData["Title"] = "MasterContactUsInformationIndex";
}

<div class="pagetitle">
    <h1>Contact Us Table</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Home">Home</a></li>
            <li class="breadcrumb-item active">Contact Us</li>
        </ol>
    </nav>
</div>


<hr />


<div class="text-end">
    <p>
        <a asp-action="MasterContactUsInformationCreate" class="btn btn-outline-success"><span>+</span> Create Contact Us</a>
    </p>
</div>


<section class="section">
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    <table class="table datatable index-table text-break">
                        <thead>
                            <tr >
                                <th>
                                    @Html.DisplayNameFor(model => model.MasterContactUsInformationIdesc)
                                </th>
                                <th class="w-25">
                                    @Html.DisplayNameFor(model => model.MasterContactUsInformationRedirect)
                                </th>
                                <th class="w-25">
                                    @Html.DisplayNameFor(model => model.MasterContactUsInformationImageUrl)
                                </th>
                                <th class="text-center">
                                    Actions
                                </th>
                            </tr>
                        </thead>
                        <tbody >
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="align-middle">
                                        @Html.DisplayFor(modelItem => item.MasterContactUsInformationIdesc)
                                    </td>
                                    <td class="align-middle">
                                        @Html.DisplayFor(modelItem => item.MasterContactUsInformationRedirect)
                                    </td>
                                    <td class="align-middle w-25">
                                        @Html.DisplayFor(modelItem => item.MasterContactUsInformationImageUrl)
                                    </td>
                                    <td class="align-middle text-center">
                                        <a asp-action="MasterContactUsInformationEdit" asp-route-id="@item.MasterContactUsInformationId">
                                            <i class="bx bxs-edit text-secondary h5"></i>
                                        </a>|
                                        <a href="" data-bs-toggle="modal" data-bs-target="#deleteModal" class="csBtnDelete"
                                           data-path="@Url.ActionLink("MasterContactUsInformationIndex", "MasterContactUsInformation", new { idDelete = item.MasterContactUsInformationId })">
                                            <i class="bx bxs-trash text-danger h5"></i>
                                        </a>|
                                        @if (item.IsActive)
                                        {
                                            <a asp-action="Active" asp-route-id="@item.MasterContactUsInformationId">
                                                <i class="ri-eye-fill text-success h5"></i>
                                            </a>
                                        }
                                        else
                                        {
                                            <a asp-action="Active" asp-route-id="@item.MasterContactUsInformationId">
                                                <i class="ri-eye-close-fill text-danger h5"></i>
                                            </a>
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>


<div class="modal fade" id="deleteModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Confirm Delete</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Are you sure for delete this item ?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-success" id="btnCansel" data-bs-dismiss="modal">Close</button>
                <button type="button" class="btn btn-danger" id="btnConfirmDelete">Delete</button>
            </div>
        </div>
    </div>
</div>

<script>
    $(function () {

        var dataPath = "";
        $(".csBtnDelete").click(function () {
            dataPath = $(this).data("path");

        });
        $("#btnConfirmDelete").click(function () {
            window.location = dataPath;
            dataPath = "";

        });

        $("#btnCansel").click(function () {
            dataPath = "";
        });

    })
</script>